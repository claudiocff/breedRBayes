% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_eta.R
\name{.component_design}
\alias{.component_design}
\title{Build the design + metadata for a single interaction component}
\usage{
.component_design(comp, data, relmat, role, exp_var = NA_real_, solo = TRUE)
}
\arguments{
\item{exp_var}{Explained-variance target for the genomic PC rotation (GBLUP
\code{vm()} / \code{mrk()}); \code{NA} keeps every non-null component.}

\item{solo}{\code{TRUE} when the component is the whole term (not part of an
interaction). A solo fixed factor is given full "cell-means" coding (one
column per level, no reference dropped) so every level appears in
\code{\link[=solution]{solution()}}; factors inside an interaction keep treatment contrasts to stay
identifiable against the intercept and the main effects.}
}
\description{
Build the design + metadata for a single interaction component
}
\keyword{internal}
