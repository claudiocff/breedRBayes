% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_fw}
\alias{plot_fw}
\title{Plot Finlay–Wilkinson regression lines highlighting Top-N and checks}
\usage{
plot_fw(post_prob, top_n = 10)
}
\arguments{
\item{post_prob}{An object produced by \code{prob_sup_bglr()}. It must contain
\code{post_prob$across$reg}, a data frame with columns:
\itemize{
\item \code{gradient}: environmental index values,
\item \code{value}: predicted response along the gradient,
\item \code{ID}: genotype ID,
\item \code{check}: logical flag indicating checks.
}}

\item{top_n}{Integer. Number of top genotypes to highlight (default = 10).}
}
\value{
A ggplot object.
}
\description{
Draws genotype-specific regression profiles across the environment gradient,
highlighting the Top-N genotypes (by mean predicted value across the gradient)
and the check genotypes.
}
\details{
Input compatibility:
\itemize{
\item Uses \code{post_prob$across$reg} created from the Legendre basis (\code{gradient_basis})
in \code{prob_sup_bglr}, where predictions across the adaptive gradient are computed as
\eqn{\hat{y}_{g}(l) = \sum_{d=0}^{D} \beta_{g,d}\,\mathrm{deg}_d(l)},
with \code{deg0 = 1} and \code{deg1..degD} being orthonormalized Legendre basis columns.
\item The Top-N genotypes are defined by the highest mean of \code{value} across the gradient.
\item Lines are colored and thickened for \code{TopN} and \code{Check} categories; others are thin and grey.
\item Labels are added at the last gradient point only for highlighted lines.
\item The legend labels adapt dynamically to \code{top_n} (e.g., "Top10", "Top30").
}
}
\examples{
\donttest{
# Assuming you already ran prob_sup_bglr():
p1 <- plot_fw(post_prob = probs, top_n = 10)
print(p1)
}

}
\seealso{
prob_sup_bglr
}
