% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_engine.R
\name{bbglr}
\alias{bbglr}
\title{Fit a Bayesian mixed model with the BGLR engine using asreml-style formulas}
\usage{
bbglr(
  fixed,
  random = NULL,
  residual = NULL,
  data,
  relmat = list(),
  control = bbglr_control(),
  ...
)
}
\arguments{
\item{fixed}{Two-sided formula for the fixed/mean part, e.g. \code{yield ~ 1 + env}.
Use \code{cbind(t1, t2) ~ ...} for a multi-trait model.}

\item{random}{One-sided formula of random terms, e.g. \code{~ mrk(gen, M)}. Special
functions: \code{mrk(f, M, method)} genomic effect from a \strong{marker matrix}
\code{M} that automatically fits GBLUP or RR-BLUP, whichever is cheaper (\code{method}
is \code{"auto"}, \code{"GBLUP"} or \code{"RRBLUP"}; \code{"auto"} picks GBLUP when markers >=
genotypes, else RR-BLUP — the two are prediction-equivalent). In GBLUP mode
the genomic relationship is fitted through a low-rank principal-component
rotation keeping the leading components that explain \code{exp_var_rank} of the
genomic variance (see \code{\link[=bbglr_control]{bbglr_control()}}, default 0.99); when \pkg{RSpectra}
is installed this rotation is computed straight from the markers without ever
forming the \verb{n x n} relationship matrix (much cheaper for many genotypes). A
\code{mrk()} fit
can score new genotypes with \code{\link[=predict.breedRB_fit]{predict.breedRB_fit()}} and recover marker
effects with \code{\link[=solve_SNP]{solve_SNP()}}; it is the recommended genomic term. \code{vm(f, K)}
effect of factor \code{f} with a supplied covariance matrix \code{K} — use it for a
kernel you already have and cannot rebuild from markers (pedigree A-matrix,
environmic kernel, externally-computed G, or any custom kernel). \code{leg(x, n)}
/ \code{rr(x, n)} random regression of order \code{n}; \code{fa(f, k)} / \code{rrc(f, k)}
factor-analytic. Bare names are factors; \code{a:b} is an interaction.}

\item{residual}{One-sided residual formula. \code{NULL}/\code{~ units} = homogeneous;
\code{~ dsum(~units | env)} = heterogeneous residual variances by \code{env}.}

\item{data}{A data frame.}

\item{relmat}{Named list of matrices referenced by \code{mrk()} and \code{vm()}. For
\code{mrk()} the entry is a marker matrix with genotypes in rows (row names =
genotype IDs) and markers in columns, coded as \strong{0/1/2 allele dosages}.
Missing values are mean-imputed per marker and (near-)constant markers are
dropped automatically. In GBLUP mode \code{mrk()} then builds the genomic
relationship internally by VanRaden's method 1 (\eqn{G = ZZ'/2\sum p_j q_j})
and adds a tiny diagonal ridge so \code{G} is always non-singular. For \code{vm()} the entry is a covariance / kernel matrix
(K, not its inverse) — genomic, pedigree, environmic or any custom kernel —
with row/column names matching the factor levels.}

\item{control}{A \code{\link[=bbglr_control]{bbglr_control()}} object bundling the MCMC controls (\code{nIter},
\code{burnIn}, \code{thin}, \code{nChains}, \code{seed}, \code{saveAt}, \code{verbose}) and model
hyperparameters (\code{exp_var_rank}, the explained-variance target for the
genomic low-rank rotation).}

\item{...}{Individual \code{\link[=bbglr_control]{bbglr_control()}} settings passed directly as a shortcut
(e.g. \code{nIter = 20000}); they override the corresponding value in \code{control}.}
}
\value{
An object of class \code{breedRB_fit}.
}
\description{
Fit a Bayesian mixed model with the BGLR engine using asreml-style formulas
}
\examples{
\donttest{
G <- readRDS(system.file("extdata", "kinship.matrix.rds", package = "breedRBayes"))
dat <- read.csv(system.file("extdata", "data_soy.csv", package = "breedRBayes"))
fit <- bbglr(yield ~ 1 + env, random = ~ vm(gen, G), data = dat,
             relmat = list(G = G),
             control = bbglr_control(nIter = 2000, burnIn = 500, nChains = 2))
}
}
\seealso{
\code{\link[=bbglr_control]{bbglr_control()}} for the available control settings.
}
