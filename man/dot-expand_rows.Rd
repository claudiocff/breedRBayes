% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.expand_rows}
\alias{.expand_rows}
\title{Expand a level-indexed matrix to observation rows by gathering}
\usage{
.expand_rows(M, f, levels)
}
\arguments{
\item{M}{Matrix with one row per level (row order matching \code{levels}).}

\item{f}{Vector of observations, coercible to a factor with these \code{levels}.}

\item{levels}{Character vector of levels, in the same order as \code{rows(M)}.}
}
\value{
A matrix with one row per element of \code{f}.
}
\description{
Equivalent to \code{.incidence(f) \%*\% M} (the 0/1 factor design of \code{f} times a
matrix \code{M} whose rows are indexed by \code{levels}), but computed as a pure
row-gather: no dense \verb{N x nLevel} incidence matrix is built and no matrix
product is formed. Row \code{i} of the result is \code{M[level_of(f_i), ]}. This is
numerically identical to the incidence product (an incidence matrix only
selects rows) but avoids the \verb{O(N x nLevel)} allocation and the
\verb{O(N x nLevel x ncol(M))} multiply — the main memory cost under replication.
}
\keyword{internal}
