% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_rm.R
\name{outlier_rm.default}
\alias{outlier_rm.default}
\title{Screen a raw response for outliers (formula / vector method)}
\usage{
\method{outlier_rm}{default}(
  x,
  data = NULL,
  by = NULL,
  k = 3,
  action = c("na", "remove", "flag"),
  min_n = 5L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A formula \verb{response ~ group1 + group2 ...} (screening within the
crossed strata of the right-hand-side factors), a one-sided formula
\code{response ~ 1} / \code{~ response} (screen the whole column), or a numeric vector.}

\item{data}{A \code{data.frame} holding the variables in \code{x}. Omit when \code{x} is a
numeric vector.}

\item{by}{Optional grouping, as an alternative to putting factors on the
formula's right-hand side: a factor / vector (vector input) or a character
vector of column names (data-frame input). Combined with any RHS factors.}

\item{k}{Threshold in robust standard deviations. \code{3} is a common default;
larger is more permissive. Must be positive.}

\item{action}{What to do with flagged observations: \code{"na"} sets the response to
\code{NA} (default; keeps every row, so the design stays aligned), \code{"remove"}
drops the offending rows, \code{"flag"} changes nothing but returns the flags.}

\item{min_n}{Strata with fewer than this many non-missing observations are left
untouched (the MAD is unreliable for tiny groups). Default \code{5}.}

\item{verbose}{Logical; print a one-line summary of how many were flagged.}

\item{...}{Unused.}
}
\value{
For a \code{data.frame} input, the data frame after applying \code{action}
(\code{"flag"} returns it unchanged); an added logical column
\verb{<response>_outlier} marks the flagged rows when \code{action != "remove"}. For a
vector input, the cleaned numeric vector, or the logical flag vector when
\code{action = "flag"}. In all cases the result carries an \code{"outliers"} attribute:
a list with the logical \code{flag}, the integer \code{index} of flagged rows, the
robust \code{score}s and the \code{k} used.
}
\description{
The response and any grouping factors follow the package's formula convention:
the left-hand side is the trait to screen and the (optional) right-hand side
lists factors defining the strata within which the median and MAD are computed
— e.g. \code{yield ~ env} screens for outliers separately in each environment. A
bare numeric vector is also accepted (with \code{data = NULL}).
}
\examples{
data(wheat, package = "breedRBayes")
# screen yield within each environment, blanking outliers to NA
clean <- outlier_rm(yield ~ env, data = wheat, k = 3)
# or drop the rows entirely
outlier_rm(yield ~ env, data = wheat, k = 4, action = "remove")
# plain vector
outlier_rm(c(1, 2, 3, 4, 100))
}
