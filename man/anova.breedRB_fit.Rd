% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_compare.R
\name{anova.breedRB_fit}
\alias{anova.breedRB_fit}
\title{Compare fitted models: WAIC / PSIS-LOO, DIC, and an approximate deviance test}
\usage{
\method{anova}{breedRB_fit}(object, ..., loo = TRUE)
}
\arguments{
\item{object}{A \code{breedRB_fit}.}

\item{...}{Further \code{breedRB_fit} objects to compare against \code{object}.}

\item{loo}{Logical; compute PSIS-LOO in addition to WAIC (requires the
\pkg{loo} package). Default \code{TRUE}.}
}
\value{
An object of class \code{breedRB_anova} (a list with \code{table}, the deviance
\code{lrt} data frame, and — when available — the \code{loo_compare} matrix), with a
\code{print} method.
}
\description{
Compares two or more models fitted with \code{\link[=bbglr]{bbglr()}} — typically a ladder of
random-regression reaction norms of increasing Legendre degree — using the
criteria appropriate for a Bayesian fit. It reports, per model, the deviance
information criterion (\code{DIC}) and effective number of parameters (\code{pD}), the
widely-applicable information criterion (\code{WAIC}), and, when the \pkg{loo}
package is installed, PSIS-LOO (\code{LOOIC}) with the standard error of each
pairwise difference (via \code{\link[loo:loo_compare]{loo::loo_compare()}}) — the most defensible
"is-the-extra-degree-worth-it" comparison here. Lower \code{DIC}/\code{WAIC}/\code{LOOIC} is
better.
}
\details{
It also prints an \strong{approximate deviance ratio} between consecutive models (in
the order supplied): the drop in posterior-mean deviance \code{Dbar = DIC - pD}
against a chi-square with degrees of freedom equal to the increase in \code{pD}.
This is a rough heuristic only. A true likelihood-ratio test does not apply to
these Bayesian fits, and even under REML the added random-regression
(co)variances are tested on the boundary of the parameter space, so the naive
chi-square reference distribution is wrong (a chi-bar-squared mixture is
needed). Prefer the WAIC/LOO comparison and its difference SE; treat the
deviance ratio and its "p-value" as indicative, not a formal test.

Pass the models in order of increasing complexity (degree 0, 1, 2, ...) so the
consecutive deviance ratios line up with adding one Legendre degree at a time.
}
\examples{
\donttest{
m0 <- bbglr(y ~ leg(x), random = ~ mrk(gen, M), data = dat, relmat = list(M = M))
m1 <- bbglr(y ~ leg(x), random = ~ mrk(gen, M) + mrk(gen, M):leg(x, 1),
            data = dat, relmat = list(M = M))
m2 <- bbglr(y ~ leg(x), random = ~ mrk(gen, M) + mrk(gen, M):leg(x, 2),
            data = dat, relmat = list(M = M))
anova(m0, m1, m2)
}
}
\seealso{
\code{\link[=model_fit]{model_fit()}}, \code{\link[=heritability]{heritability()}}, \code{\link[=varcomp]{varcomp()}}.
}
