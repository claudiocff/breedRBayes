% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{fitted.breedRB_fit}
\alias{fitted.breedRB_fit}
\title{Fitted values from a \code{breedRB_fit}}
\usage{
\method{fitted}{breedRB_fit}(object, ...)
}
\arguments{
\item{object}{A \code{breedRB_fit}.}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A numeric vector of fitted values (single-trait), or an
\verb{n x nTrait} matrix (multi-trait). The per-observation posterior SD
(\code{SD.yHat}, averaged across chains) is attached as attribute \code{"sd"}.
}
\description{
Posterior-mean fitted values from the BGLR engine (\code{yHat}), pooled across
chains. These include \strong{every} model term — the fixed part, all random terms
and, for a random regression, the fitted reaction norm at each observation's
covariate value. Rows are aligned to the data actually modelled
(\code{fit$data}), so missing responses (which BGLR imputes) still receive a fitted
value.
}
\seealso{
\code{\link[=residuals.breedRB_fit]{residuals.breedRB_fit()}}, \code{\link[=model_fit]{model_fit()}}.
}
