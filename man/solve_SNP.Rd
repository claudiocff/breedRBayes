% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{solve_SNP}
\alias{solve_SNP}
\title{Marker (SNP) effects from a genomic model}
\usage{
solve_SNP(fit, M = NULL, term = NULL, prob = 0.95)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait) with a \code{vm()} or \code{mrk()} genomic term.}

\item{M}{Marker matrix with genotypes in rows (row names matching the genotype
IDs) and markers in columns. Only needed to override the training markers
when back-solving a GBLUP fit; for a \code{mrk()} fit the training markers stored
in the fit are used automatically, and a RR-BLUP fit ignores \code{M}.}

\item{term}{Genomic term identifier (label or key). Defaults to the first
genomic term.}

\item{prob}{Central credible-interval mass (default 0.95).}
}
\value{
A data frame with \code{marker}, \code{effect} (posterior mean), \code{sd}, \code{lower},
\code{upper}, in marker (design) order, with the pooled draws matrix
(\verb{nDraws x nMarker}) attached as attribute \code{"draws"} and the fitted method
as attribute \code{"method"}.
}
\description{
Recovers the posterior distribution of per-marker allele-substitution effects
\eqn{b} on the centred-marker scale, such that the genomic breeding values
satisfy \eqn{GEBV = M_c\, b} (where \eqn{M_c} is the column-centred marker
matrix). This lets a \strong{GBLUP} fit — which estimates breeding values rather
than marker effects — be back-transformed to marker effects:
\deqn{b = M_c^\top (M_c M_c^\top)^{-1}\, u}
applied to every posterior draw of the breeding values \eqn{u}. For a
\strong{RR-BLUP} fit (\code{mrk()} chose RR-BLUP because genotypes outnumbered markers)
the marker effects are estimated directly and are simply rescaled and returned.
}
\details{
The back-transform requires \eqn{M_c M_c^\top} to be invertible, which holds
when the number of markers is at least the number of genotypes — exactly the
regime in which \code{mrk()} selects GBLUP.
}
\examples{
\donttest{
snp <- solve_SNP(fit)      # marker effects (back-solved for GBLUP, direct for RR-BLUP)
head(snp[order(-abs(snp$effect)), ])
}
}
\seealso{
\code{\link[=predict.breedRB_fit]{predict.breedRB_fit()}} to score new genotypes, \code{\link[=solution]{solution()}}.
}
