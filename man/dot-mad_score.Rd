% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_rm.R
\name{.mad_score}
\alias{.mad_score}
\title{MAD score of a numeric vector about its median}
\usage{
.mad_score(x, constant = 1.4826)
}
\arguments{
\item{x}{Numeric vector (may contain \code{NA}, which score as \code{NA}).}

\item{constant}{Consistency constant making the scaled MAD an unbiased
estimator of the standard deviation under normality (\code{1.4826}).}
}
\value{
A numeric vector of robust z-scores, the same length as \code{x}.
}
\description{
Robust standardised distance \verb{|x - median(x)| / (constant * MAD)}, the basis of
the Hampel / median-absolute-deviation outlier rule. When the MAD is zero (more
than half the values are identical, so the scaled MAD collapses) the spread is
re-estimated from the mean absolute deviation about the median, itself rescaled
to be consistent for the normal. If both are zero (a constant vector) every
score is \code{0} — nothing is flagged.
}
\keyword{internal}
