% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_formula.R
\name{parse_model}
\alias{parse_model}
\title{Parse asreml-style fixed / random / residual formulas}
\usage{
parse_model(fixed, random = NULL, residual = NULL)
}
\arguments{
\item{fixed}{Two-sided formula for the mean/fixed part, e.g.
\code{yield ~ 1 + env}. Multi-trait models use \code{cbind(t1, t2) ~ ...}.}

\item{random}{One-sided formula for random terms, e.g.
\code{~ vm(gen, G) + env:vm(gen, G)}. \code{NULL} for none.}

\item{residual}{One-sided residual formula. \code{NULL} (default) or \code{~ units}
gives homogeneous residuals; \code{~ dsum(~units | env)} gives heterogeneous
residual variances by \code{env}.}
}
\value{
A \code{breedRB_terms} list: \code{response}, \code{fixed}, \code{random}, \code{residual}.
}
\description{
Turns the three model formulas into an internal, engine-agnostic description
used by \code{\link[=bbglr]{bbglr()}}. Not usually called directly.
}
\examples{
parse_model(yield ~ 1 + env, ~ vm(gen, G))
}
