% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{bbglr_control}
\alias{bbglr_control}
\title{Control settings for \code{\link[=bbglr]{bbglr()}}}
\usage{
bbglr_control(
  nIter = 5000,
  burnIn = 1000,
  thin = 5,
  nChains = 1,
  seed = 123,
  saveAt = NULL,
  verbose = TRUE,
  exp_var_rank = 0.99
)
}
\arguments{
\item{nIter, burnIn, thin}{MCMC controls passed to \pkg{BGLR}: total iterations,
burn-in, and thinning interval.}

\item{nChains}{Integer number of independent chains (distinct seeds). Enables
Gelman-Rubin diagnostics when \verb{> 1}.}

\item{seed}{Base random seed; chain \code{c} uses \code{seed + c - 1}.}

\item{saveAt}{Directory for BGLR binary output. \code{NULL} (default) uses a fresh
temporary directory.}

\item{verbose}{Logical; print BGLR progress.}

\item{exp_var_rank}{Genomic low-rank control for GBLUP-style terms
(\code{mrk()} in GBLUP mode and \code{vm()}). A number in \verb{(0, 1]} keeps the smallest
set of leading principal components of the genomic relationship that together
explain at least this fraction of its total variance (default \code{0.99}), giving
a principled, self-scaling low-rank / PC-GBLUP fit that shrinks the design
and speeds up sampling while retaining essentially all of the genetic signal.
Set to \code{1} or \code{NA} to keep every (non-null) component. An explicit integer
\verb{rank =} inside \code{mrk()} overrides this for that term.}
}
\value{
A list of class \code{breedRB_control}.
}
\description{
Bundles the MCMC controls and model hyperparameters into a single object, so
the main \code{\link[=bbglr]{bbglr()}} call stays short. Pass it as \code{control = bbglr_control(...)};
any value left at its default is used as-is.
}
\examples{
bbglr_control(nIter = 20000, burnIn = 5000, exp_var_rank = 0.95)
}
\seealso{
\code{\link[=bbglr]{bbglr()}}
}
