% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{.random_prior_var}
\alias{.random_prior_var}
\title{Prior variance of each level/coefficient of a random term}
\usage{
.random_prior_var(fit, key, meta)
}
\value{
A named numeric vector of prior variances, one per effect column.
}
\description{
The denominator of a BLUP reliability: \eqn{\mathrm{Var}(u_i) = D_{ii}\,\sigma^2_u},
where \eqn{\sigma^2_u} is the term's variance component (posterior mean of its
\code{varB} chain) and \eqn{D_{ii}} is the prior relationship diagonal for level \code{i}.
For a genomic term (\verb{u = PC\\,b}, \verb{PC PC' = G}) this is \eqn{G_{ii}\,\sigma^2_u}
with \eqn{G_{ii} = \sum_k \mathrm{PC}_{ik}^2}; for a plain random factor or a
random-regression basis (\eqn{D = I}) it is simply \eqn{\sigma^2_u} for every
level.
}
\keyword{internal}
