% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_rm.R
\name{outlier_rm.breedRB_fit}
\alias{outlier_rm.breedRB_fit}
\title{Remove outliers from a fitted model by screening its residuals}
\usage{
\method{outlier_rm}{breedRB_fit}(
  x,
  k = 3,
  action = c("remove", "na", "flag"),
  by = NULL,
  min_n = 5L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{breedRB_fit} from \code{\link[=bbglr]{bbglr()}}.}

\item{k}{Threshold in robust standard deviations (default \code{3}; larger is more
permissive). Must be positive.}

\item{action}{What to do with a flagged record: \code{"remove"} (default) drops the
row, \code{"na"} sets the offending response(s) to \code{NA} (keeping the row so the
design stays aligned) and adds a \verb{<trait>_outlier} marker column, \code{"flag"}
changes nothing but adds the marker column(s).}

\item{by}{Optional grouping for the residual screen: a character vector of
\code{fit$data} column names, or a factor / vector aligned to the modelled rows.
Residuals are usually mean-zero across the model's strata already, so a global
screen (the default, \code{NULL}) is normally what you want; use \code{by} only to allow
a different residual scale per group.}

\item{min_n}{Strata with fewer than this many finite residuals are left
untouched. Default \code{5}.}

\item{verbose}{Logical; print a one-line summary of how many were flagged.}

\item{...}{Unused.}
}
\value{
The model's data frame (\code{fit$data}) after applying \code{action}: rows
removed for \code{"remove"}, else the same rows with the response(s) blanked
(\code{"na"}) or unchanged (\code{"flag"}) plus a logical \verb{<trait>_outlier} column. The
result carries an \code{"outliers"} attribute — a list with the logical \code{flag}
(per modelled row), the integer \code{index} of flagged rows, the screened
\code{residuals}, the robust \code{score}s and the \code{k} used.
}
\description{
Screens the \strong{residuals} of a fitted \code{\link[=bbglr]{bbglr()}} model — observed minus
posterior-mean fitted value (\code{\link[=residuals.breedRB_fit]{residuals.breedRB_fit()}}) — with the robust MAD
rule, and returns the model's data frame (\code{fit$data}) with the offending
observations removed (or blanked / flagged). Because the residual already
subtracts everything the model explains (the fixed part, the genomic / random
effects, and for a random regression the fitted reaction norm), an extreme
residual is genuine lack of fit for that record rather than a real design
effect — the appropriate quantity to screen before refitting.
}
\details{
The usual workflow is to fit once, drop the residual outliers, and refit on the
returned data frame:
\preformatted{
  fit   <- bbglr(yield ~ 1 + env, random = ~ mrk(gen, M), data = dat,
                 relmat = list(M = M))
  clean <- outlier_rm(fit, k = 3)          # residual-based, returns a data.frame
  fit2  <- bbglr(yield ~ 1 + env, random = ~ mrk(gen, M), data = clean,
                 relmat = list(M = M))
}

For a multi-trait fit each trait's residual column is screened separately; a
row is flagged when \strong{any} trait residual is an outlier. \code{action = "na"}
blanks only the offending trait cell(s); \code{action = "remove"} drops the whole
row.
}
\seealso{
\code{\link[=residuals.breedRB_fit]{residuals.breedRB_fit()}}, \code{\link[=fitted.breedRB_fit]{fitted.breedRB_fit()}}, \code{\link[=model_fit]{model_fit()}},
\code{\link[=outlier_rm.default]{outlier_rm.default()}}.
}
