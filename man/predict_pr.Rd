% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probability.R
\name{predict_pr}
\alias{predict_pr}
\title{Posterior probability that new genotypes exceed a training-population threshold}
\usage{
predict_pr(
  fit,
  M_new,
  threshold = 0.1,
  term = NULL,
  higher = TRUE,
  add_mu = FALSE,
  prob = 0.95
)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait) with a \code{mrk()} genomic term.}

\item{M_new}{Marker matrix for the genotypes to score: genotype IDs in the row
names, markers in columns (same requirements as \code{\link[=predict.breedRB_fit]{predict.breedRB_fit()}};
missing calls are imputed with the training mean).}

\item{threshold}{Training-population fraction in \verb{(0, 1)} defining the bar;
\code{0.10} = the top 10\% of the training population (its 90th percentile when
\code{higher = TRUE}).}

\item{term}{Genomic term identifier (label or key). Defaults to the first
genomic term.}

\item{higher}{Logical (default \code{TRUE}). If \code{TRUE} the bar is the upper
\code{threshold} tail (larger is better, e.g. yield); set \code{FALSE} when smaller
values are better (then it is the lower tail).}

\item{add_mu}{Logical (default \code{FALSE}). Whether the reported \code{prediction}
column is on the overall-mean scale (\code{+ mu}) or the deviation scale. Does not
affect \code{prob} (the intercept cancels in the comparison).}

\item{prob}{Central credible-interval mass for the underlying prediction
(default 0.95); affects only the internal prediction, not \code{prob}.}
}
\value{
A data frame with \code{ID}, \code{prediction} (posterior mean predicted value),
\code{sd} (posterior SD of the prediction — the genotype's predictive
uncertainty), and \code{prob} (posterior probability of clearing the
training-population bar), ordered by decreasing \code{prob}. The pooled
new-genotype draws are attached as attribute \code{"draws"}, and the
posterior-mean bar as attribute \code{"cutoff"}.
}
\description{
Scores genotypes that were \strong{not} in the training data (from their markers,
via \code{\link[=predict.breedRB_fit]{predict.breedRB_fit()}}) and reports, for each, the posterior probability
that its genomic value clears a bar defined by the \strong{training population} —
e.g. "probability this new line beats the top 10\% of the training set".
}
\details{
The calculation is fully Bayesian and done per posterior draw: in each MCMC
draw the bar is the corresponding quantile of the \emph{training} genotypes'
breeding values in that draw (so the threshold itself carries uncertainty),
and the new genotype's predicted value in that same draw is compared against
it. The reported probability is the fraction of draws the genotype clears the
bar. Because the whole posterior is used, a new genotype with \strong{more marker
information} (a tighter predictive posterior) that sits above the bar earns a
higher probability than an equally-ranked but more uncertain one — two lines
with the same point prediction can have very different probabilities.

Requires a marker-based \code{mrk()} term (a \code{vm()} fit cannot score new
genotypes). The probability is invariant to \code{add_mu}: the intercept shifts the
new value and the training bar equally, so it cancels in the comparison.
}
\examples{
\donttest{
fit <- bbglr(yield ~ 1, random = ~ mrk(gen, M), data = dat, relmat = list(M = M))
# P(each new line beats the top 10\% of the training population)
predict_pr(fit, M_new, threshold = 0.10)
}
}
\seealso{
\code{\link[=predict.breedRB_fit]{predict.breedRB_fit()}} for the point predictions, \code{\link[=pr]{pr()}} for the
in-sample analogue.
}
