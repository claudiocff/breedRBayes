% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_rm.R
\name{outlier_rm}
\alias{outlier_rm}
\title{Robust MAD-based outlier detection and removal}
\usage{
outlier_rm(x, ...)
}
\arguments{
\item{x}{A \code{breedRB_fit}, a \code{formula}, or a numeric vector — see the methods.}

\item{...}{Passed to methods.}
}
\value{
The cleaned data (see each method); always carries an \code{"outliers"}
attribute describing what was flagged.
}
\description{
Flags observations that lie more than \code{k} robust standard deviations from the
median, using the median absolute deviation (MAD) rule
\verb{|value - median| / (1.4826 * MAD) > k}. Unlike mean/standard-deviation
screening, the median and MAD are not themselves dragged around by the outliers
they are meant to catch, which makes the rule well suited to breeding-trial
data.
}
\details{
\code{outlier_rm()} is generic. Screen a raw response with the \link[stats:formula]{formula}
/ vector method (\code{\link[=outlier_rm.default]{outlier_rm.default()}}), or screen the \strong{residuals of a fitted
model} with \code{\link[=outlier_rm.breedRB_fit]{outlier_rm.breedRB_fit()}} — the latter accounts for the fixed and
random structure already in the model, so what remains to be judged is genuine
lack of fit rather than, say, a real environment effect.
}
\seealso{
\code{\link[=outlier_rm.breedRB_fit]{outlier_rm.breedRB_fit()}}, \code{\link[=outlier_rm.default]{outlier_rm.default()}}.
}
