% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reaction_norm.R
\name{.rr_leg_parts}
\alias{.rr_leg_parts}
\title{Detect any random-regression interaction (\verb{grouping x leg()})}
\usage{
.rr_leg_parts(meta)
}
\value{
\code{NULL} unless the term is a two-way interaction of one \code{leg()} basis
with exactly one grouping component; otherwise a list with that component
\code{gc}, the \code{leg()} order \code{q}, its variable \code{legvar} and \code{range}, and whether
the grouping is \code{genomic}.
}
\description{
Broader than \code{\link[=.geno_leg_parts]{.geno_leg_parts()}}: the grouping component may be a genomic
\code{vm()}/\code{mrk()} effect \strong{or} a plain random \code{factor} (e.g. \code{gen:leg(x)} with
\code{gen} fitted without a relationship matrix). \code{solution()} already returns
per-level reaction-norm coefficients for both.
}
\keyword{internal}
