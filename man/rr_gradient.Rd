% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rr_gradient.R
\name{rr_gradient}
\alias{rr_gradient}
\title{Reaction-norm analytics across the environmental gradient}
\usage{
rr_gradient(
  fit,
  term,
  n_grid = 20L,
  threshold = 0.2,
  higher = TRUE,
  leg_basis = TRUE,
  top_n = NULL,
  prob = 0.95,
  plot = TRUE
)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait) containing a random regression.}

\item{term}{The interaction term identifier, e.g. \code{"gen:leg(x)"} or
\code{"mrk(gen, M):leg(x, 1)"}. Its matching intercept term is detected
automatically.}

\item{n_grid}{Integer; number of gradient points (default 20). The
correlation surface is \verb{n_grid x n_grid}; keep it modest for large panels.}

\item{threshold}{Selected fraction in \verb{(0, 1)} for the selection
probabilities; \code{0.20} = top 20\%.}

\item{higher}{Logical (default \code{TRUE}); if \code{FALSE}, "top" means the smallest
values (used for both the selection probability and genotype ordering).}

\item{leg_basis}{Logical (default \code{TRUE}). Gradient axis on the standardized
Legendre domain \eqn{[-1, 1]}; if \code{FALSE}, back-transformed to the covariate
scale stored at fitting.}

\item{top_n}{Optional integer; restrict the per-genotype reliability and
probability heat maps to the \code{top_n} genotypes (by mean value across the
gradient) so the figures stay legible. \code{NULL} (default) keeps all genotypes.}

\item{prob}{Central credible-interval mass for the heritability band
(default 0.95).}

\item{plot}{Logical (default \code{TRUE}); draw and print the four \pkg{ggplot2}
figures. They are always returned in the \code{plots} element.}
}
\value{
An object of class \code{breedRB_rrgradient} (a list) with: \code{grid} (gradient
points on the chosen scale), \code{K} (posterior-mean coefficient (co)variance),
\code{gcov} / \code{gcor} (\verb{n_grid x n_grid} genetic covariance / correlation
surfaces), \code{h2} (data frame \code{gradient}, \code{mean}, \code{median}, \code{lower}, \code{upper}),
\code{reliability} and \code{prob_top} (\verb{nGeno x n_grid} matrices), and \code{plots} (named
list of ggplots: \code{cor}, \code{h2}, \code{reliability}, \code{prob}).
}
\description{
For a random-regression (reaction-norm) fit, summarises the fitted
reaction-norm coefficients across the environmental gradient. Writing each
genotype's curve as \eqn{v_i(x) = \phi(x)^\top c_i} with
\eqn{\phi(x) = (1, L_1(x), \dots, L_q(x))} (intercept column = 1, then the
orthonormal Legendre slopes) and coefficient vector
\eqn{c_i = (a_i, b_{i,1}, \dots, b_{i,q})}, it returns:
}
\details{
\itemize{
\item \strong{Genetic covariance / correlation surface.} With
\eqn{K = \mathrm{Var}(c_i)} the (co)variance matrix of the reaction-norm
coefficients across genotypes (intercept--slope covariance included), the
genetic covariance between two gradient points is
\eqn{G(x_1, x_2) = \phi(x_1)^\top K\, \phi(x_2)}, i.e. the matrix
\eqn{\Phi K \Phi^\top}; \code{cov2cor()} of it is the across-gradient genetic
\strong{correlation} surface (how a genotype ranking at one environment carries
over to another).
\item \strong{Heritability over the gradient.} \eqn{h^2(x) = \sigma^2_g(x) /
  (\sigma^2_g(x) + \sigma^2_e)} with \eqn{\sigma^2_g(x) = \phi(x)^\top K \phi(x)}
the diagonal of the covariance surface and \eqn{\sigma^2_e} the residual
variance, computed per MCMC draw for a full posterior (mean + credible band).
\item \strong{Reliability over the gradient.} Per genotype,
\eqn{r^2_i(x) = 1 - \mathrm{PEV}_i(x)/\sigma^2_g(x)}, where
\eqn{\mathrm{PEV}_i(x)} is the posterior variance of \eqn{v_i(x)}.
\item \strong{Selection probability over the gradient.} For each gradient point, the
posterior probability that each genotype ranks in the top \code{threshold}
fraction \emph{at that point} (each MCMC draw ranked independently, as in \code{\link[=pr]{pr()}}).
}

\code{K} is estimated from the posterior draws of the per-genotype coefficients
(the realised across-genotype coefficient (co)variance, averaged over draws),
so it carries an intercept--slope covariance even though \code{~ gen + gen:leg(x)}
fits the two terms with independent priors.
}
\examples{
\donttest{
fit <- bbglr(yield ~ leg(x), random = ~ gen + gen:leg(x) + env:rep, data = dat)
g <- rr_gradient(fit, term = "gen:leg(x)", threshold = 0.20)
g$gcor          # across-gradient genetic correlation matrix
g$plots$prob    # who is likely to be top-20\% where along the gradient
}
}
\seealso{
\code{\link[=reaction_norm]{reaction_norm()}} for the per-genotype curves, \code{\link[=pr]{pr()}} for the
single-point selection probabilities, \code{\link[=heritability]{heritability()}}.
}
