% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_pair_prob}
\alias{plot_pair_prob}
\title{Heatmap of pairwise probabilities of superior performance (Top-N)}
\usage{
plot_pair_prob(post_prob, top_n = 30)
}
\arguments{
\item{post_prob}{Output from \code{prob_sup_bglr()} containing:
\itemize{
\item \code{across$bayesian_stats} (for ranking by \code{prob_top}),
\item \code{across$pair_perfo} (square matrix of pairwise probabilities),
\item \code{control$increase} (flag indicating whether higher is better), typically set in \code{prob_sup_bglr}.
}}

\item{top_n}{Integer. Number of top genotypes to display. Default = 30.}
}
\value{
A ggplot object.
}
\description{
Visualizes pairwise probabilities among the Top-N genotypes using the
matrix of pairwise performance probabilities computed in \code{prob_sup_bglr()}.
}
\details{
\itemize{
\item The function subsets the pairwise matrix to the Top-N genotypes and masks the
lower triangle and diagonal for clearer visualization.
\item The legend label is determined by the \code{increase} flag:
\code{"Pr(X > Y)"} if higher is better, otherwise \code{"Pr(X < Y)"}.
}
}
\examples{
\donttest{
p6 <- plot_pair_prob(post_prob = probs, top_n = 30)
print(p6)
}

}
\seealso{
prob_sup_bglr
}
