% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_param}
\alias{plot_param}
\title{Scatter of FW parameters: slope vs intercept colored by R2}
\usage{
plot_param(post_prob)
}
\arguments{
\item{post_prob}{Output from \code{prob_sup_bglr()} containing \code{across$bayesian_stats}
with columns \code{slope}, \code{intercept}, and an \code{R2} column.}
}
\value{
A ggplot object.
}
\description{
Visualizes genotype-level FW parameters with slope on the x-axis and intercept on the y-axis,
coloring by an R2 statistic (e.g., goodness-of-fit of the FW regression per genotype).
}
\details{
Legendre-compatible:
\itemize{
\item The slope here corresponds to the degree-1 coefficient from the Legendre random regression
(as reported in \code{bayesian_stats$slope}). Intercept is \eqn{\alpha_g}.
\item This function assumes that \code{post_prob$across$bayesian_stats} contains an \code{R2} column.
If \code{R2} was not computed earlier, you should add it before calling this function
(e.g., based on observed vs fitted FW lines per genotype).
}
}
\examples{
\donttest{
# If bayesian_stats has an R2 column:
p4 <- plot_param(post_prob = probs)
print(p4)
}

}
\seealso{
prob_sup_bglr
}
