% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fw_extract.R
\name{extr_outs_bglr}
\alias{extr_outs_bglr}
\title{Extract posterior summaries, Legendre random regression coefficients,
variance components, diagnostics, and predictive profiles from FW_bglr}
\usage{
extr_outs_bglr(model, gen)
}
\arguments{
\item{model}{An object of class \code{bglr_met} returned by \code{FW_bglr}, including its
\code{path} (saveAt), posterior effect matrices (\code{INT}, \code{COEF}, \code{SLOPE}, \code{GE}, \code{GGE}),
and metadata (\code{data}, \code{trait}, \code{env}, \code{order}, \code{check}, \code{X_gradient}, \code{range}).}

\item{gen}{Character. Column name for genotype IDs, matching the column used in \code{FW_bglr}.}
}
\value{
An object of class \code{extr_bglr} (list) containing:
\itemize{
\item \code{post}: list of posterior chains:
\itemize{
\item \code{chain_int} \link{nGen x nIter}: genotype intercepts.
\item \code{chain_coef}: list of degree-specific chains (\code{slope1..slopeD}), each \link{nGen x nIter}.
\item \code{chain_slope}: first-degree chain for backward compatibility (may be NULL if \code{order==0}).
\item \code{chain_ge}: centered genotype-by-environment predictions across iterations.
\item \code{chain_gge}: uncentered genotype + GE predictions across iterations.
}
\item \code{variances}: data frame with posterior means and SDs of variance components:
intercept, environment main effect (\code{l}), each slope_d, and error (from Step 2).
\item \code{diagnostic_trace}: data frame with traces for \code{mu}, \code{intercept}, \code{l},
each \verb{slope\{d\}}, and iteration index.
\item \code{effective_sizes}: numeric vector of effective sample sizes for diagnostic traces
computed via \code{coda::effectiveSize}.
\item \code{gradient}: matrix with two columns (intercept=1, gradient values) spanning the
range of the environment effect (\code{l}) for legacy compatibility.
\item \code{gradient_basis}: Legendre basis matrix (deg0..degD) on the adaptive grid returned by
\code{.make_gradient_basis}, suitable for evaluating profile curves.
\item \code{G_param}: data frame with genotype-level posterior means for \code{intercept} and
\verb{slope\{d\}} (degrees 1..D), plus a \code{check} flag.
\item \code{N_obs}: data frame with counts of observations per genotype (\code{ID}, \code{Obs}).
\item \code{check}: vector of check genotype IDs propagated from the model.
\item \code{ID_R2}: data frame with per-genotype R^2 (squared correlation across environments
between predicted and observed means).
}
}
\description{
Summarizes posterior chains, variance components, diagnostic traces, and
genotype-level Finlay–Wilkinson random regression parameters (intercept and
Legendre coefficients) from a model fitted by \code{FW_bglr}. Also constructs
environment-by-genotype predictions and per-genotype R^2 against observed means.
}
\details{
Conceptual model with Legendre basis and separate intercept:
\eqn{y_{ge} = \mu + l_e + \alpha_g + \sum_{d=1}^{D} \gamma_{g,d}\,\tilde P_d(x_e) + \varepsilon_{ge}}
where \eqn{x_e} is the environment index scaled to \link{-1,1}, \eqn{\tilde P_d} are
orthonormal Legendre polynomials for degrees \eqn{d=1,\dots,D}, and \eqn{\alpha_g}
is the genotype-specific intercept. The function collects posterior chains for
\eqn{\alpha_g} and \eqn{\gamma_{g,d}}, variance components, and diagnostics.

Files read for diagnostics:
\itemize{
\item \code{mu.dat}, \code{ETA_int_varB.dat}, \code{ETA_l_varB.dat}, and \verb{ETA_slope\{d\}_varB.dat} (for each degree).
\item Ensure \code{FW_bglr} was run with \code{saveAt} pointing to a writable directory that ends
with a path separator (e.g., \code{"/"}), so these files exist at extraction time.
}

Predictions and R^2:
\itemize{
\item Environment-by-genotype predictions use the model's environmental basis \code{X_gradient}
(columns \code{deg0..degD}, with \code{deg0=1}) and posterior mean coefficients
(\code{intercept} and \verb{slope\{d\}}).
\item Observed means by genotype×environment are computed from \code{model$data}, and R^2 is
the squared correlation between predictions and observed means across environments per genotype.
}
}
\examples{
\donttest{
# Fit FW_bglr first (see FW_bglr examples), then:
# ext <- extr_outs_bglr(model = fit_fw, gen = "gid")
# names(ext)
# head(ext$G_param)
# ext$variances
# ext$effective_sizes
# str(ext$gradient_basis)
}

}
\seealso{
FW_bglr, .make_gradient_basis, legendre_basis
}
