% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gxe.R
\name{gxe}
\alias{gxe}
\title{Genotype-by-environment summary of a random-regression fit}
\usage{
gxe(fit, term, n_grid = 100L, add_fixed_reg = TRUE, higher = TRUE)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait) containing a random regression.}

\item{term}{The interaction term identifier, e.g. \code{"gen:leg(x)"} or
\code{"mrk(gen, M):leg(x, 1)"}; its matching intercept term is detected
automatically (as in \code{\link[=reaction_norm]{reaction_norm()}}).}

\item{n_grid}{Integer; number of gradient points used to evaluate each curve
(default 100).}

\item{add_fixed_reg}{Logical (default \code{TRUE}); evaluate the curves on the
phenotype scale (\code{mu} + fixed population regression added) so \code{cv_ge} has a
meaningful mean in the denominator.}

\item{higher}{Logical (default \code{TRUE}); if \code{TRUE} a higher value is better
(adaptability rank 1 = highest); set \code{FALSE} to rank the smallest as best.}
}
\value{
A data frame of class \code{breedRB_gxe}, one row per genotype ordered by
broad adaptability, with columns \code{id}, \code{adaptability} (overall level),
\code{responsiveness} (gradient slope), \code{cv_ge} (coefficient of variation, \\%),
\code{adaptability_rank} and \code{stability_rank} (rank 1 = most stable, i.e.
smallest \code{cv_ge}).
}
\description{
Reduces each genotype's fitted reaction norm \eqn{v_i(x)} across the
environmental gradient to three interpretable genotype-by-environment
descriptors:
}
\details{
\itemize{
\item \strong{Broad adaptability} — the genotype intercept, i.e. its overall
performance level across the gradient (mean of its curve). Higher = better
general performance.
\item \strong{Responsiveness} — the slope of the genotype's curve along the gradient
(the linear reaction-norm slope; for a \code{leg()} order \eqn{> 1} the average
slope from a straight-line fit to the curve). Larger magnitude = more
sensitive to the environment; near zero = flat / broadly adapted.
\item \strong{Stability (\code{cv_ge})} — the coefficient of variation of the genotype's
curve across the gradient, \eqn{100 \times \mathrm{sd}(v_i(x)) /
  |\mathrm{mean}(v_i(x))|}. \strong{Lower \code{cv_ge} means a more stable genotype}
(flatter curve relative to its level).
}

The curves are the posterior-mean per-genotype reaction norms from
\code{\link[=reaction_norm]{reaction_norm()}}. \code{cv_ge} is only meaningful on the phenotype scale, so
\code{add_fixed_reg = TRUE} (the default) is recommended.
}
\examples{
\donttest{
fit <- bbglr(yield ~ leg(x), random = ~ gen + gen:leg(x) + env:rep, data = dat)
gxe(fit, term = "gen:leg(x)")
}
}
\seealso{
\code{\link[=reaction_norm]{reaction_norm()}} for the curves and \code{\link[=rr_gradient]{rr_gradient()}} for the
across-gradient (co)variance / heritability / selection analytics.
}
