% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probability.R
\name{pr}
\alias{pr}
\title{Posterior probability of ranking in the top fraction}
\usage{
pr(fit, term = NULL, type = NULL, threshold = 0.2, higher = TRUE)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait).}

\item{term}{Term identifier (label as written in the formula, e.g. \code{"gen"}, or
the internal key). If \code{NULL}, the first random term is used.}

\item{type}{Optional; one of \code{"random"} or \code{"fixed"}, validated against the
term's actual role. For a fixed term fitted with treatment contrasts the
reference level is not among the coefficients and is therefore excluded.}

\item{threshold}{Selected fraction in \verb{(0, 1)}; \code{0.20} = top 20\%.}

\item{higher}{Logical (default \code{TRUE}). If \code{TRUE} the "top" is the highest
values (e.g. yield); set \code{FALSE} when smaller values are better.}
}
\value{
A data frame with \code{effect} (level name), \code{solution} (posterior mean),
and \code{prob} (posterior probability of ranking in the selected fraction),
ordered by decreasing \code{prob}. The number of selected levels per draw
(\code{k}) and the \code{threshold} are attached as attributes.
}
\description{
For a model term, compute the posterior probability that each level ranks
among the best \code{threshold} fraction of levels. Working from the pooled
posterior draws (see \code{\link[=solution]{solution()}}), each MCMC draw is ranked independently and
the top \eqn{k = round(threshold \times n)} levels are flagged; the reported
probability is the fraction of draws in which a level is flagged. This is the
Bayesian "probability of being in the top X\\%" used to rank selection
candidates by their whole posterior, not just their point estimate.
}
\details{
Because every draw is ranked on its own, adding the intercept \code{mu} would not
change the ranking, so \code{pr()} is unaffected by centering.
}
\examples{
\donttest{
# probability each genotype is in the top 20\%
pr(fit, term = "gen", type = "random", threshold = 0.20)
}
}
\seealso{
\code{\link[=solution]{solution()}} for the underlying posterior draws.
}
