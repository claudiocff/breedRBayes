% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.svds_expvar}
\alias{.svds_expvar}
\title{Leading eigenpairs of \verb{Mc Mc'} explaining a variance target, without the tail}
\usage{
.svds_expvar(
  Mc,
  exp_var,
  total = sum(rowSums(Mc^2)),
  k0 = NULL,
  growth = 2,
  k_max = NULL
)
}
\arguments{
\item{Mc}{Column-centred marker matrix (\code{n} genotypes x \code{p} markers).}

\item{exp_var}{Target cumulative variance fraction in \verb{(0, 1)}.}

\item{total}{Total variance (\code{sum(rowSums(Mc^2))}); pass it if already known.}

\item{k0}{Initial block size; grown by \code{growth} each round.}

\item{growth}{Multiplicative block-growth factor (\verb{> 1}).}

\item{k_max}{Largest \code{k} to attempt before giving up (defaults to half the
spectrum). Beyond this a full eigendecomposition is cheaper.}
}
\value{
A list with \code{vectors} (top eigenvectors of \verb{Mc Mc'}) and \code{evals} (their
eigenvalues), truncated to the target; or \code{NULL} to signal "fall back to a
full eigendecomposition".
}
\description{
Finds the smallest set of top principal components of the genomic relationship
that explain at least \code{exp_var} of its total variance, \strong{without decomposing
the full spectrum}. The trick: the total variance is the trace of \verb{Mc Mc'},
i.e. \code{sum(rowSums(Mc^2))} — available in \code{O(np)} with no decomposition — so it
is the exact denominator up front. Only the top eigenpairs are then computed,
grown a block at a time with \code{RSpectra::svds} (which never forms \verb{Mc Mc'}),
stopping as soon as the running cumulative share crosses the target.
}
\details{
Cheap when the relationship has strong low-rank structure (a handful of PCs
dominate, as in real genomic panels): only a small \verb{k << n} is ever computed.
If the target would require more than about half the spectrum (little
structure), the search gives up and returns \code{NULL} so the caller can fall back
to a single full eigendecomposition — which is cheaper than svds at that point.
}
\keyword{internal}
