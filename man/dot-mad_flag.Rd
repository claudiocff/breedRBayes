% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_rm.R
\name{.mad_flag}
\alias{.mad_flag}
\title{MAD outlier flags computed within strata}
\usage{
.mad_flag(y, grp, k, min_n)
}
\arguments{
\item{y}{Numeric vector.}

\item{grp}{Grouping factor, same length as \code{y}.}

\item{k}{Threshold in robust standard deviations.}

\item{min_n}{Minimum finite observations per stratum to attempt scoring.}
}
\value{
A list with the logical \code{flag} and numeric \code{score}, both length \code{y}.
}
\description{
Scores \code{y} with \code{\link[=.mad_score]{.mad_score()}} separately inside each level of \code{grp} and flags
values beyond \code{k}. Strata with fewer than \code{min_n} finite values are left
unscored (score \code{0}, never flagged) because the MAD is unreliable for tiny
groups. \code{NA} values score \code{NA} and are never flagged.
}
\keyword{internal}
