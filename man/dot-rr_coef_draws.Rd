% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rr_gradient.R
\name{.rr_coef_draws}
\alias{.rr_coef_draws}
\title{Per-draw reaction-norm coefficients (intercept + per-degree slopes)}
\usage{
.rr_coef_draws(fit, term)
}
\value{
A list with \code{ids}, \code{q}, \code{legvar}, \code{range}, \code{genomic}, the intercept
draws \code{A} (\verb{[nDraws x nGeno]}) and a length-\code{q} list \code{B} of per-degree slope
draws (each \verb{[nDraws x nGeno]}, columns in the \code{ids} order).
}
\description{
Pulls the pooled posterior draws of the grouping main effect (the intercept
\eqn{a_i}) and of the \code{leg()} interaction (\code{term}, the per-degree slopes
\eqn{b_{i,j}}) and reshapes them into per-genotype coefficient draw matrices,
all aligned to a common genotype order. Works for both genomic
(\code{mrk(gen, M):leg(x, q)}) and plain-factor (\code{gen:leg(x)}) random regressions.
}
\keyword{internal}
