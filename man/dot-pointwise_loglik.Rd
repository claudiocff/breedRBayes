% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_compare.R
\name{.pointwise_loglik}
\alias{.pointwise_loglik}
\title{Pointwise log-likelihood matrix (draws x observations) for a Gaussian fit}
\usage{
.pointwise_loglik(fit)
}
\arguments{
\item{fit}{A single-trait \code{breedRB_fit} (homogeneous or heterogeneous
residual).}
}
\value{
A numeric matrix \verb{[nDraws x nObs]} of pointwise log-likelihoods, with
attributes \code{"chain_id"} (chain of each draw row), \code{"S_per_chain"} and
\code{"obs_index"} (which rows of \code{fit$data} the columns correspond to).
}
\description{
Reconstructs the per-draw linear predictor and evaluates the Gaussian
pointwise log-likelihood \verb{log p(y_i | theta^(s))} for every retained MCMC
draw \code{s}, over the observations with a non-missing response. This is the input
WAIC and PSIS-LOO need.
}
\details{
Deterministic term designs (fixed effects, plain random factors, Legendre
bases, covariates, and their interactions) are rebuilt directly and multiplied
by BGLR's saved coefficient draws. \code{vm()} / \code{mrk()} genomic terms are \strong{not}
rebuilt — their principal-component rotation is data-dependent and, under the
default \code{RSpectra} truncation, not sign-reproducible — so their per-observation
contribution is taken from \code{\link[=solution]{solution()}}, which back-maps through the rotation
stored on the fit. Draws are pooled across chains in \code{fit$paths} order,
matching how \code{\link[=solution]{solution()}} pools, so all pieces align draw-by-draw.

Heterogeneous residuals (\code{dsum(~units | g)}) are supported: each observation
is scored against its own residual-variance group's per-draw variance.
}
\keyword{internal}
