% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{predict.breedRB_fit}
\alias{predict.breedRB_fit}
\title{Predict genomic values for new genotypes from their markers}
\usage{
\method{predict}{breedRB_fit}(object, M_new, term = NULL, add_mu = TRUE, prob = 0.95, ...)
}
\arguments{
\item{object}{A \code{breedRB_fit} (single-trait) with a \code{mrk()} genomic term.}

\item{M_new}{Marker matrix for the genotypes to predict: genotype IDs in the
row names, markers in columns. Must contain every marker used in the fit
(extra columns are ignored; column order need not match). May include
training genotypes, in which case the prediction reproduces their fitted
value.}

\item{term}{Genomic term identifier (label or key). Defaults to the first
genomic term.}

\item{add_mu}{Logical (default \code{TRUE}). Add the model intercept \code{mu} to every
draw, putting predictions on the overall-mean (phenotype) scale rather than
the deviation scale. Done per posterior draw, so \code{mu}'s uncertainty enters
the interval.}

\item{prob}{Central credible-interval mass (default 0.95).}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A data frame with \code{ID}, \code{prediction} (posterior mean), \code{sd}, \code{lower},
\code{upper}, ordered by decreasing \code{prediction}, with the pooled draws matrix
(\verb{nDraws x nGenotype}) attached as attribute \code{"draws"}.
}
\description{
Scores genotypes that were \strong{not} in the training data by combining the
posterior marker effects with the new genotypes' marker calls:
\deqn{\hat{g}_{new} = M_{c,new}\, b,}
where \eqn{M_{c,new}} is \code{M_new} centred by the \strong{training} column means (so
the new genotypes sit on the same scale as the fitted breeding values) and
\eqn{b} are the per-draw marker effects (read directly for a RR-BLUP fit,
back-solved for a GBLUP fit — see \code{\link[=solve_SNP]{solve_SNP()}}). The full posterior is
propagated, so every returned prediction carries a credible interval.
}
\details{
This requires a marker-based \code{mrk()} term. A \code{vm()} fit holds only a
relationship matrix and cannot score genotypes outside it; refit the genomic
term as \code{mrk(gen, M)} to enable prediction.
}
\examples{
\donttest{
fit  <- bbglr(y ~ 1, random = ~ mrk(gen, M), data = dat, relmat = list(M = M))
pred <- predict(fit, M_new, add_mu = TRUE)   # score unobserved genotypes
head(pred)
}
}
\seealso{
\code{\link[=solve_SNP]{solve_SNP()}}, \code{\link[=solution]{solution()}}.
}
