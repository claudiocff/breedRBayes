% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.clean_markers}
\alias{.clean_markers}
\title{Clean a marker matrix before building a genomic model}
\usage{
.clean_markers(M, var_tol = 1e-08, var_name = "gen")
}
\arguments{
\item{M}{Numeric marker matrix (genotypes in rows, markers in columns).}

\item{var_tol}{Markers with variance below this are treated as constant and
removed.}

\item{var_name}{Factor name, used only for informative messages.}
}
\value{
The cleaned marker matrix (possibly with fewer columns).
}
\description{
Mean-imputes missing values (per marker/column) and drops markers with
(near-)zero variance, so the downstream GBLUP / RR-BLUP construction never
sees \code{NA}s or constant columns. Markers that are entirely missing become
non-finite after imputation and are dropped alongside the constant ones.
}
\keyword{internal}
