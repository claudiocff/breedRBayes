% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{wheat}
\alias{wheat}
\title{CIMMYT wheat multi-environment yield trial}
\format{
A data frame with 2396 rows (599 lines x 4 environments) and 3
columns:
\describe{
\item{gen}{Line identifier (factor, 599 levels), matching \code{rownames(wheat_M)}.}
\item{env}{Environment (factor, \code{E1}..\code{E4}).}
\item{yield}{Standardised grain yield (one record per line x environment).}
}
}
\source{
Distributed with the \pkg{BGLR} package; originally from CIMMYT's
Global Wheat Program. See Crossa et al. (2010) \emph{Genetics} 186:713--724.
}
\usage{
wheat
}
\description{
Grain yield of 599 CIMMYT wheat lines evaluated in four environments, in
long format ready for the \pkg{breedRBayes} formula interface. This is the
long-format reshape of the classic \code{wheat} dataset distributed with
\pkg{BGLR} (\code{wheat.Y}); the yields are the pre-standardised values from that
source. The matching marker matrix is \link{wheat_M} and the two share the same
genotype identifiers, so the same data serve the phenotype-only
(\code{random = ~ gen}) and genomic (\code{random = ~ mrk(gen, wheat_M)}) analyses.
}
\examples{
\donttest{
data(wheat)
fit <- bbglr(yield ~ env, random = ~ gen, data = wheat)
heritability(fit, genetic = "gen")
}
}
\seealso{
\link{wheat_M} for the marker matrix.
}
\keyword{datasets}
