% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reaction_norm.R
\name{reaction_norm}
\alias{reaction_norm}
\title{Genotype reaction-norm curves across the environmental gradient}
\usage{
reaction_norm(
  fit,
  term,
  type = NULL,
  add_fixed_reg = TRUE,
  plot = TRUE,
  leg_basis = TRUE,
  n_grid = 100L
)
}
\arguments{
\item{fit}{A \code{breedRB_fit} (single-trait) containing a random regression.}

\item{term}{The interaction term identifier, e.g. \code{"gen:leg(x)"} or
\code{"mrk(gen, M):leg(x, 1)"} (label as written in the formula, or the internal
key). Its matching intercept term (\code{gen} / \code{mrk(gen, M)}) is detected
automatically.}

\item{type}{Optional; validated against the term's role (must be \code{"random"}
for a reaction norm).}

\item{add_fixed_reg}{Logical (default \code{TRUE}). Add the fixed population
regression (\code{mu} + the fixed \code{leg()} curve on the same gradient) so the
curves sit on the phenotype scale; otherwise curves are genotype deviations
around zero.}

\item{plot}{Logical (default \code{TRUE}). Draw and print a \pkg{ggplot2} figure of
value versus gradient, one line per genotype. The plot is also returned as
attribute \code{"plot"}.}

\item{leg_basis}{Logical (default \code{TRUE}). If \code{TRUE} the gradient axis is the
standardized Legendre domain \eqn{[-1, 1]}; if \code{FALSE} it is back-transformed
to the original covariate scale using the range stored at fitting.}

\item{n_grid}{Integer; number of gradient points (default 100).}
}
\value{
A long-format data frame with \code{id} (genotype), \code{gradient} (the
evaluation point, on the chosen scale) and \code{value} (fitted reaction-norm
value). The gradient grid is attached as attribute \code{"grid"} and, when
\code{plot = TRUE}, the \pkg{ggplot2} object as attribute \code{"plot"}.
}
\description{
For a random-regression (reaction-norm) fit, evaluates each genotype's fitted
curve over the environmental gradient,
\deqn{v_i(x) = \hat a_i + \sum_{j=1}^{q} \hat b_{i,j}\, L_j(x),}
where the intercept \eqn{\hat a_i} comes from the grouping main-effect term and
the reaction-norm coefficients \eqn{\hat b_{i,j}} from the interaction \code{term}
(\code{deg1} = linear slope, \code{deg2} = quadratic, ...), and \eqn{L_j} is the same
orthonormal Legendre basis used in fitting, on the standardized domain
\eqn{[-1, 1]}. The grouping factor may be genomic
(\code{~ mrk(gen, M) + mrk(gen, M):leg(x, q)}) or a plain random factor fitted
without a relationship matrix (\code{~ gen + gen:leg(x)}).
}
\examples{
\donttest{
fit <- bbglr(y ~ 1 + leg(x, 1),
             random = ~ mrk(gen, M) + mrk(gen, M):leg(x, 1),
             data = dat, relmat = list(M = M))
rn <- reaction_norm(fit, term = "mrk(gen, M):leg(x, 1)", type = "random")
}
}
\seealso{
\code{\link[=solution]{solution()}} for the underlying intercept and slope coefficients.
}
