% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fw_extract.R
\name{.make_gradient_basis}
\alias{.make_gradient_basis}
\title{Adaptive environment grid and Legendre basis for profile evaluation}
\usage{
.make_gradient_basis(
  model,
  min_points = 50,
  step_fraction = 0.01,
  use_scale = c("range", "sd", "IQR")
)
}
\arguments{
\item{model}{A \code{bglr_met} object returned by \code{FW_bglr}. Must contain:
\code{data}, \code{trait}, \code{range} (the environment-index range from Step 1),
and \code{order} (maximum Legendre degree used in Step 2).}

\item{min_points}{Integer. Minimum number of points in the grid (default 50).}

\item{step_fraction}{Numeric in (0,1). Fraction of the response scale used to
set the maximum step size in the environment grid (default 0.01).}

\item{use_scale}{Character. Which response scale to use to set grid resolution:
one of \code{c("range","sd","IQR")}. Default \code{"range"}.}
}
\value{
A list with:
\itemize{
\item \code{grid}: numeric vector of environment index values (length L).
\item \code{basis}: matrix of Legendre basis values with columns named \code{deg0..degD},
where \code{deg0} is constant 1 and \code{deg1..degD} are orthonormalized Legendre values.
}
}
\description{
Builds an adaptive grid on the environment index (l) and the corresponding
Legendre polynomial basis used to evaluate genotype-specific profiles.
The grid resolution is tied to the response scale, and the basis is
orthonormalized for degrees 1..D while forcing degree 0 to be 1 to match
the separate genotype intercept used in the Finlay–Wilkinson random regression.
}
\details{
Conceptual background:
\itemize{
\item Finlay–Wilkinson random regression with Legendre basis (intercept separated):
#$# y_{ge} = \mu + l_e + \alpha_g + \sum_{d=1}^{D} \gamma_{g,d}\,\tilde P_d(x_e) + \varepsilon_{ge} #$#
where #$#x_e#$# is the scaled environment index in \link{-1,1}, #$#\tilde P_d#$# are orthonormal Legendre polynomials.
\item Orthonormalization of Legendre polynomials on \link{-1,1}:
#$# \tilde P_n(x) = \sqrt{\frac{2n+1}{2}}\,P_n(x), \quad \int_{-1}^{1}\tilde P_n(x)^2\,dx = 1 #$#.
For compatibility with a separate intercept, the degree-0 column is set to 1.
}

The environment index grid is constructed over \code{model$range} with a step size
bounded by \code{resp_scale * step_fraction}, where \code{resp_scale} is computed from
the response variable using the chosen scale (\code{range}, \code{sd}, or \code{IQR}).
The grid values are then scaled to \link{-1,1} and fed to \code{legendre_basis(...)}.
The first column (degree 0) is replaced by 1 to ensure that genotype intercepts
multiply a constant column in predictions.
}
\examples{
\donttest{
# Assuming you have fitted FW_bglr and obtained `fit_fw`:
# gb <- .make_gradient_basis(fit_fw, min_points = 100, step_fraction = 0.02, use_scale = "sd")
# str(gb$grid)
# str(gb$basis)
}

}
\seealso{
FW_bglr, legendre_basis
}
\keyword{internal}
